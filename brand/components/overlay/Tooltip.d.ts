/** Hover/focus tooltip above the child. Keep labels one short line. */
export interface TooltipProps {
  label: React.ReactNode;
  children?: React.ReactNode;
}
export declare function Tooltip(props: TooltipProps): JSX.Element;
