Underline tab bar with optional mono count pills (差异分组、任务分类).

```jsx
<Tabs active={t} onChange={setT} items={[
  {key:"auto",label:"可自动执行",count:823},
  {key:"manual",label:"需人工",count:12},
  {key:"conflict",label:"冲突",count:6},
]}/>
```
