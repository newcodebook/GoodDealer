Text field with caps label, ink inset surface, blue focus ring.

```jsx
<Input label="BIN 价格" mono prefix="$" placeholder="0.00" suffix="USD"/>
<Input label="搜索" prefix={<SearchIcon/>} placeholder="域名、标签、注册商…"/>
```

`mono` for prices/records/domains. `error="…"` shows danger border + message.
