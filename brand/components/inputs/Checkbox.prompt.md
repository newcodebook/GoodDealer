Checkbox with indeterminate tri-state (table headers).

```jsx
<Checkbox checked={all} indeterminate={some} onChange={toggleAll}/>
<Checkbox label="包含平台 API 凭据" checked={v} onChange={e=>setV(e.target.checked)}/>
```
