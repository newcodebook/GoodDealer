{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-app",
  "description": "Local application shell with core permissions and the read-only runtime status command.",
  "webviews": ["local-app"],
  "permissions": ["core:default", "allow-runtime-status"]
}
