{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-app",
  "description": "Local application shell with only Tauri core permissions during WP-0.",
  "windows": ["local-app"],
  "permissions": ["core:default"]
}
