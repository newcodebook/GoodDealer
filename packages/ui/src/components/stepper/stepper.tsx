import type { ReactNode } from "react";
import { CheckIcon } from "../icon/icon";
import "./stepper.css";
export interface StepperItem { key: string; label: ReactNode; description?: ReactNode }
export interface StepperProps { items: readonly StepperItem[]; activeKey?: string; activeIndex?: number; orientation?: "horizontal" | "vertical"; mark?: ReactNode; footer?: ReactNode; label?: string }
export function Stepper({ items, activeKey, activeIndex, orientation = "horizontal", mark, footer, label }: StepperProps) { const current = activeIndex ?? Math.max(0, items.findIndex((item) => item.key === activeKey)); return <ol className={`gd-stepper gd-stepper--${orientation}`} aria-label={label}>{mark ? <li className="gd-stepper-mark">{mark}</li> : null}{items.map((item, index) => { const state = index < current ? "done" : index === current ? "current" : "upcoming"; return <li key={item.key} className={`gd-stepper-item gd-stepper-item--${state}`} aria-current={state === "current" ? "step" : undefined}><span className="gd-stepper-index">{state === "done" ? <CheckIcon size={12} /> : index + 1}</span><span className="gd-stepper-copy"><span>{item.label}</span>{item.description ? <small>{item.description}</small> : null}</span>{orientation === "horizontal" && index < items.length - 1 ? <span className="gd-stepper-line" /> : null}</li>; })}{footer ? <li className="gd-stepper-footer">{footer}</li> : null}</ol>; }
